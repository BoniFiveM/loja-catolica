<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Finalizar Compra</title>
    <link href="/style.css" rel="stylesheet"> <!-- Estilo -->
</head>
<body class="bg-gray-900 text-white">
    <h1 class="text-center text-2xl mt-10">Finalizar Compra</h1>

    <% if (user) { %>
        <p class="text-center mt-4">Bem-vindo, <%= user.nome %>!</p>
    <% } else { %>
        <p class="text-center mt-4">Por favor, faça login para continuar.</p>
    <% } %>

    <h2 class="text-center text-xl mt-4">Produtos no carrinho</h2>
    <ul class="list-none p-0">
        <% produtosCarrinho.forEach(produto => { %>
            <li class="flex justify-between p-4 border-b">
                <span><%= produto.nome %> (x<%= produto.quantidade %>)</span>
                <span>R$ <%= (produto.preco * produto.quantidade).toFixed(2) %></span>
            </li>
        <% }) %>
    </ul>

    <div class="text-center mt-4">
        <p>Total: R$ <%= produtosCarrinho.reduce((total, item) => total + (item.preco * item.quantidade), 0).toFixed(2) %></p>
        <a href="/finalizar-compra" class="bg-blue-600 text-white py-2 px-4 rounded hover:bg-blue-700 mt-4">
            Finalizar Compra
        </a>
    </div>
</body>
</html>
